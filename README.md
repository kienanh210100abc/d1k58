## Version & Package

Using node >= v20 and bun (1.16) for package management, next v14

## Getting Started

First, run the development server:

```bash
bun dev
```

For build:

```bash
bun run build
```

and the build folder is out/

Open [http://localhost:3000](http://localhost:3000) with your browser to see the result.
